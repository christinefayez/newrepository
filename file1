git branch dev
git branch test
git checkout dev
touch file1
git add file1
git commit -m "add branch dev"
git checkout main
git merge dev 
git push origin dev

git tag -a v1.4 -m "new tag by christine"

 git push --tags


delete tag remotely :git push origin --delete v1.4
local:git tag -d v1.4 
